### Asistencia de Inteligencia Artificial

- **Prompts utilizados**:
  - "¿Cómo traducir listas por comprensión (list comprehensions) a map y filter en Haskell, Elixir y Python?"
  - "Corrige el formato Markdown de la investigación porque GitHub colapsa los saltos de línea y desordena los bloques de código."

- **Herramientas utilizadas**:
  - Gemini

- **Cambios y validación**:
  - Se eliminaron expresiones complejas en LaTeX (`$$...$$`) que provocaban errores de renderizado en el visor de GitHub.
  - Se reestructuraron las listas y bloques de código (`python`, `haskell`, `elixir`) asegurando saltos de línea dobles válidos en Markdown estándar.
  - Se verificó la sintaxis del cuadro comparativo en Markdown para evitar que el texto colapsara en un solo párrafo.

- **Reflexión personal**:
  La IA generó correctamente las equivalencias semánticas entre las listas por comprensión y las combinaciones de `map`/`filter`, pero requirió ajustes en la sintaxis de Markdown para evitar problemas de compatibilidad en el motor de renderizado de GitHub.

- **Fecha**: 2026-09-15
- **Plataforma utilizada**: GitHub (editor web / Markdown preview)
